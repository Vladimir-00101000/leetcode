# Команды UNIX

```bash
find . -name "*.txt"
```

```bash
cat file.txt | wc -l
```
wc -> кол-во строк, слов, байт; -l -> только кол-во строк
| -> пайп

```bash
hostname
```

```bash
echo "Hello, World!"
```

```bash
date
```

```bash
clear
```

Завершение текущей терминальной сессии
```bash
exit
```
(CTRL + D)

```bash
~ домашний каталог
$ приглашение к вводу от обычного ползователя
# приглашение к вводу от пользователя root
```

```bash

```